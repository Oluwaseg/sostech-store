<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Invitation to SOSTECH Store</title>
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background-color: #f5f5f5;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 600px;
        margin: 24px auto;
        background: #ffffff;
        border-radius: 8px;
        padding: 24px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
      }
      .badge {
        display: inline-block;
        padding: 4px 10px;
        border-radius: 999px;
        background-color: #eef4ff;
        color: #3758f9;
        font-size: 12px;
        font-weight: 600;
        letter-spacing: 0.03em;
        text-transform: uppercase;
        margin-bottom: 12px;
      }
      .button {
        display: inline-block;
        padding: 10px 18px;
        border-radius: 999px;
        background-color: #111827;
        color: #f9fafb !important;
        text-decoration: none;
        font-weight: 600;
        margin: 16px 0;
      }
      .code {
        font-family: 'SF Mono', Menlo, Monaco, Consolas, 'Liberation Mono',
          'Courier New', monospace;
        background: #111827;
        color: #f9fafb;
        padding: 8px 12px;
        border-radius: 6px;
        font-size: 14px;
        letter-spacing: 0.08em;
        display: inline-block;
        margin-top: 8px;
      }
      .footer {
        margin-top: 24px;
        font-size: 12px;
        color: #6b7280;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="badge">Invitation</div>
      <h2>{{fromName}} invited you to SOSTECH Store</h2>

      <p>
        Join SOSTECH Store using {{fromName}}&apos;s referral link and help them
        unlock exclusive discount coupons.
      </p>

      <p>
        Click the button below to create your account:
      </p>

      <p>
        <a class="button" href="{{referralLink}}" target="_blank" rel="noopener"
          >Join SOSTECH Store</a
        >
      </p>

      <p>
        Or copy this link and paste it into your browser:
        <br />
        <a href="{{referralLink}}" target="_blank" rel="noopener"
          >{{referralLink}}</a
        >
      </p>

      <p>
        Referral code:
        <span class="code">{{referralCode}}</span>
      </p>

      <p class="footer">
        If you weren&apos;t expecting this invitation, you can safely ignore
        this email.<br />
        &copy; {{currentYear}} SOSTECH Store. All rights reserved.
      </p>
    </div>
  </body>
  </html>

